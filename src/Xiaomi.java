public class Xiaomi extends Smartphones {
    public Xiaomi(String brand, String model, String operationSystem, int buildInMemoryCapacity, double screenSize) {
        setBrand(brand);
        setOperationSystem(operationSystem);
        setModel(model);
        setBuildInMemoryCapacity(buildInMemoryCapacity);
        setScreenSize(screenSize);
    }
}

