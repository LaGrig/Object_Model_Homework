public class Nokia extends Smartphones {
    public Nokia(String brand, String model, String operationSystem, int buildInMemoryCapacity, double screenSize) {
        setBrand(brand);
        setOperationSystem(operationSystem);
        setModel(model);
        setBuildInMemoryCapacity(buildInMemoryCapacity);
        setScreenSize(screenSize);
    }
}
